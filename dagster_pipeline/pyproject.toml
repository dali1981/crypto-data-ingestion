[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "binance-dagster-pipeline"
version = "0.1.0"
dependencies = [
    "dagster",
    "dagster-webserver",
]
